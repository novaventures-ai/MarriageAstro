# =============================================================================
# MarriageAstro Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# --- Supabase ---
# Get these from: https://supabase.com/dashboard → Project Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# --- Google Gemini AI ---
# Get from: https://aistudio.google.com/app/apikey
# For development only (client-side). In production, set GEMINI_API_KEY in Vercel env vars.
VITE_GEMINI_API_KEY=your-gemini-api-key-here

# --- Google Maps ---
# Get from: https://console.cloud.google.com → APIs & Services → Credentials
# Enable "Places API" and "Maps JavaScript API"
# IMPORTANT: Restrict this key in Google Cloud Console:
#   1. Go to APIs & Services → Credentials → Edit your API key
#   2. Under "Application restrictions", select "HTTP referrers (websites)"
#   3. Add your domains: marriage-astro.vercel.app/*, localhost:3001/*
#   4. Under "API restrictions", restrict to: Places API, Maps JavaScript API
#   This prevents unauthorized usage if the key is exposed in client-side code.
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# =============================================================================
# Vercel Server-Side Only (set in Vercel Dashboard → Settings → Environment Variables)
# =============================================================================
# GEMINI_API_KEY=your-gemini-api-key-here    (used by /api/gemini serverless proxy)
# SUPABASE_URL=https://your-project.supabase.co  (used by /api/keep-alive)
# SUPABASE_ANON_KEY=your-anon-key-here           (used by /api/keep-alive)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key  (used by /api/admin-users, /api/payment-webhook)

# --- Razorpay (server-side — Vercel env vars only, never expose these client-side) ---
# Get from: https://dashboard.razorpay.com → Settings → API Keys
# RAZORPAY_KEY_ID=rzp_live_xxxxxxxxxxxx
# RAZORPAY_KEY_SECRET=your-key-secret-here
# RAZORPAY_WEBHOOK_SECRET=your-webhook-secret-here

# --- Razorpay (client-side — safe to expose, key_id only, no secret) ---
# VITE_RAZORPAY_KEY_ID=rzp_live_xxxxxxxxxxxx

# --- Resend (transactional email) ---
# Get from: https://resend.com/api-keys
# RESEND_API_KEY=re_xxxxxxxxxxxx
# EMAIL_FROM=Astro Marriage <noreply@yourdomain.com>
# APP_URL=https://marriage-astro.vercel.app

# --- Sentry (error monitoring) ---
# Get from: https://sentry.io → Settings → Projects → Client Keys (DSN)
# VITE_SENTRY_DSN=https://xxxx@oxxxx.ingest.sentry.io/xxxx
# VITE_APP_VERSION=1.0.0   (set in Vercel via git SHA or tag)

# --- Sentry Source Map Upload (build-time only — set in Vercel build env) ---
# Get from: https://sentry.io → Settings → Auth Tokens
# SENTRY_AUTH_TOKEN=sntrys_xxxx
# SENTRY_ORG=your-org-slug
# SENTRY_PROJECT=marriage-astro

# --- PostHog (product analytics) ---
# Get from: https://app.posthog.com → Project Settings → Project API Key
# VITE_POSTHOG_KEY=phc_xxxxxxxxxxxx
# VITE_POSTHOG_HOST=https://app.posthog.com  (or your self-hosted URL)
